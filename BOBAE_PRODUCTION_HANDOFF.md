# 보배드림 시안 → 실제 서비스 반영 개발진 전달 문서

- 기준일: 2026-09-04
- 기준 시안 저장소: `bobaekimboae/comment-depth-comparison`
- 기준 커밋: `64d5a6a`
- 목적: 현재까지 만든 보배드림 시안을 실제 프론트엔드/백엔드 서비스에 동일하게 이식하기 위한 개발 전달 문서

## 1. 전달 목적

현재 시안은 보배드림 메인, 중고차 리스트, 화물/특장 리스트, 차량 상세, 매물등록 흐름을 빠르게 검증하기 위한 정적 프로토타입이다. 실제 서비스 반영 시에는 HTML/CSS를 그대로 복사하는 방식이 아니라, 아래 기준으로 제품 코드에 이식해야 한다.

- 시안의 UI/UX, 타이포그래피, 간격, 버튼, 필터 상태, 바텀시트 동작을 실제 컴포넌트로 분해한다.
- 좌측 필터, 상단 필터칩, 모바일/우측 바텀시트는 같은 상태와 같은 필터 메타데이터를 바라보게 한다.
- 승용 중고차 필터와 화물/특장 필터는 공통 필터 엔진을 쓰되, 데이터셋과 노출 조건은 분리한다.
- 매물등록은 카테고리 선택 → 차량번호 조회 → 상세 입력 → 사진/옵션/가격 입력 → 검수/등록 흐름으로 실제 API와 연결한다.
- 개발진은 시안을 참고하되, 최종 기준은 실제 서비스의 디자인 시스템, 접근성, 성능, API 계약에 맞춘다.

## 2. 기준 시안 URL

- 메인: https://bobaekimboae.github.io/comment-depth-comparison/bobaedream-main.html
- 중고차 리스트: https://bobaekimboae.github.io/comment-depth-comparison/bobaedream-used-car-list.html
- 화물/특장 리스트: https://bobaekimboae.github.io/comment-depth-comparison/bobaedream-used-car-list.html?category=truck
- 매물등록 카테고리 선택: https://bobaekimboae.github.io/comment-depth-comparison/bobaedream-car-register.html
- 매물등록 입력폼: https://bobaekimboae.github.io/comment-depth-comparison/bobaedream-car-register.html#form

## 3. 화면별 반영 범위

### 3.1 메인

메인은 Kaidee, AutoScout24, AutoTrader의 구조를 참고해 차량 카테고리 중심 진입 화면으로 설계했다. GNB 위에 별도 패밀리 사이트를 중복 배치하지 않고, GNB 자체가 보배드림의 멀티 차량 카테고리를 담당한다.

최종 GNB 기준:

```text
전체차량
숏폼카
국산차
수입차
트럭-특장
매물등록
딜러
부품·용품
커뮤니티
더보기
```

개발 시 체크포인트:

- 보배드림 타이틀은 기존 색상과 브랜드 톤을 유지한다.
- 헤더에는 불필요한 구분선을 넣지 않고, 간격과 정렬로 위계를 만든다.
- 상단 우측 메뉴는 즐겨찾기, 채팅 문의, 매물등록, 고객센터, 로그인/회원가입, 딜러 자유이용권 등으로 정돈한다.
- 차량 카테고리 이미지는 현재 시안의 실사형 이미지 기준으로 교체하되, 각 이미지의 시각 중심과 원형/칩 배경 크기를 맞춘다.

### 3.2 중고차 리스트

중고차 리스트는 엔카 국산차 검색 구조를 참고하되, 보배드림 시안의 디자인 언어를 유지한다. 필터 UI는 Airbnb 렌터카 필터의 버튼, 체크박스, 선택 칩, 바텀시트 스타일을 응용한다.

반영 항목:

- 상단 필터칩에 좌측 필터의 모든 조건을 동일하게 노출한다.
- 좌측 필터와 바텀시트 필터는 같은 항목, 같은 선택 상태, 같은 초기화/적용 동작을 가져야 한다.
- 제조사 검색에는 브랜드 아이콘을 붙일 수 있게 구조를 열어둔다.
- 가격은 검정 계열로 정리하고, 판매자/지역 정보는 분리한다.
- 지역 정보에서 상사명은 일괄 제거한다.
- 판매자 정보는 `이은호 딜러 · 보배드림 모터스 · 판매완료 12대` 형식처럼 딜러명, 상사명, 판매완료 건수를 구분한다.
- 차량 스펙에서 변속기 자리는 마력으로 대체한다.
- 상세정보에서 노란 강조 색상은 보배드림 블루 계열로 변경한다.
- 스크롤 시 상단 필터칩이 불필요하게 고정 노출되지 않도록 한다.
- 좌측 필터는 스크롤 중 흔들리거나 잘리지 않도록 sticky 영역과 콘텐츠 높이를 명확히 분리한다.

필터 항목 기준:

- 제조사/모델
- 연식 범위
- 주행거리 범위
- 가격 범위
- 지역
- 연료
- 색상
- 옵션
- 사고/보험/성능 관련 조건
- 판매자 유형
- 사진/동영상 여부
- 정렬

엔카 진단, 엔카 보증처럼 특정 플랫폼 전용 명칭은 보배드림에 그대로 이식하지 않는다.

### 3.3 화물/특장 리스트

화물/특장 리스트는 엔카 화물/특장 필터와 동일한 정보 구조를 목표로 한다. 승용차 필터를 재사용하는 것이 아니라, 상용차에 맞는 필터와 트리 구조를 별도로 구성해야 한다.

핵심 구조:

- 형식 선택 시 상위 노드를 먼저 선택한다.
- 선택된 상위 노드는 엔카처럼 상단에 고정 표시하고, 우측에 닫기 버튼을 둔다.
- 상위 선택 후 하위 노드가 단계적으로 열린다.
- 적재용량은 형식과 연동되는 하위 조건으로 구성한다.
- 좌측 필터와 우측/모바일 바텀시트 필터는 동일한 트리와 선택 상태를 사용한다.

화물/특장 필터 기준:

- 제조사/모델
- 형식
- 세부 형식
- 적재용량
- 연식
- 주행거리
- 가격
- 지역
- 연료
- 변속기 또는 구동/축 조건
- 톤수/용도
- 판매자 유형
- 사진/동영상 여부
- 정렬

제거할 항목:

- 엔카 진단
- 엔카 보증
- 승용차 전용 옵션
- 승용차 전용 차체/등급 조건

기준 데이터:

- `보배드림 화물·특장·버스 유형·적재용량 마스터_정리본 (엔카 기준 2026-09-03).xlsx`
- `bobae_commercial_vehicle_types.json`
- `보배드림_상용차_유형_마스터데이터_v1.xlsx`
- `엔카 화물_특장_버스 필터 정보.xlsx`

### 3.4 차량 상세

차량 상세는 원본 옵션 아이콘과 상세 정보 아이콘을 기준으로 교체한다. 아이콘은 라운드 처리한 변형본이 아니라 원본 아이콘을 사용한다.

반영 항목:

- 옵션 아이콘: LED 헤드램프, 후방카메라, 어댑티브 크루즈, 어라운드뷰, 스마트키, 열선시트, 내비게이션, HUD 등
- HUD는 사용자가 제공한 `헤드업 디스플레이(HUD)s03-39-option-head-up-display.svg.webp` 기준
- 상세정보 위치/시간 아이콘
- 사진 썸네일, 이전/다음 컨트롤
- 공유, 관심, 채팅, 전화, 위치 등 공통 액션 아이콘

아이콘 적용 원칙:

- 같은 아이콘군은 같은 색상, 같은 박스 크기, 같은 optical center 기준으로 정렬한다.
- SVG 원본이 있으면 SVG를 우선 사용한다.
- WebP/PNG만 있는 경우에도 표시 크기와 여백을 통일한다.
- 버튼 안 아이콘은 실제 터치 영역과 시각 크기를 분리한다.

### 3.5 매물등록

매물등록은 `dev.bbmuseum.co.kr/car/register`의 보배드림 등록폼을 기준으로 동일한 화면과 기능을 만든다. Chotot처럼 카테고리를 먼저 선택하게 하고, 승용차 등록폼부터 실제 동작 가능하게 만든다.

현재 우선순위:

- 승용차 등록폼을 먼저 완성한다.
- 차량번호 `14러0927` 입력 시 조회 플로우를 재현한다.
- 이후 바이크, 트럭/특장 등록폼으로 확장한다.

등록 흐름:

1. 매물 유형 선택
2. 카테고리 선택
3. 차량번호 입력 및 조회
4. 기본 제원 확인
5. 연식, 주행거리, 색상, 연료 등 상세 정보 입력
6. 옵션 선택
7. 사진 등록
8. 판매가격 입력
9. 판매자 정보 확인
10. 임시저장 또는 등록 요청

UI 체크포인트:

- 판매가격 옆 `만원` 단위가 틀어지지 않아야 한다.
- 연식 등 모든 드롭다운은 클릭, 열림, 선택, 닫힘 상태가 정상 동작해야 한다.
- 바텀시트는 색상, 버튼, 체크박스, 선택 칩, 초기화 텍스트까지 시안과 동일한 방향으로 맞춘다.
- 매물 유형 카피는 사용자가 빠르게 이해하는 문장으로 정리한다.
- 카테고리 이미지는 실사형과 아이콘형을 비교 가능하게 유지하되, 최종 적용 시에는 하나의 규칙으로 통일한다.

## 4. 프론트엔드 개발 작업

프론트엔드는 시안을 아래 컴포넌트 단위로 분해해서 구현한다.

- `AppHeader`
- `GlobalNavigation`
- `TopUtilityMenu`
- `VehicleSearchHero`
- `VehicleCategoryGrid`
- `FilterChipBar`
- `FilterSidebar`
- `FilterBottomSheet`
- `FilterRangeControl`
- `FilterCheckbox`
- `SelectedFilterChip`
- `VehicleCard`
- `VehicleList`
- `CommercialVehicleTypeTree`
- `RegisterCategorySelect`
- `RegisterForm`
- `RegisterBottomSheet`
- `IconAsset`

상태 관리 원칙:

- 필터 상태는 URL query와 동기화한다.
- 좌측 필터, 상단 칩, 바텀시트는 하나의 filter state를 공유한다.
- 바텀시트에서 적용 전 임시 선택 상태가 필요하면 `draftState`와 `appliedState`를 분리한다.
- 초기화는 전체 초기화와 그룹별 초기화를 모두 지원한다.
- 범위 필터는 최소/최대값, 직접입력, 슬라이더를 같은 값으로 동기화한다.

반응형 기준:

- 데스크톱: 좌측 필터 + 중앙 리스트 + 상단 필터칩
- 태블릿: 상단 필터칩 + 접이식 필터
- 모바일: 상단 필터칩 + 바텀시트

접근성 기준:

- 모든 체크박스, 라디오, 드롭다운, 슬라이더는 키보드 조작 가능해야 한다.
- 바텀시트 열림 시 focus trap을 적용한다.
- 닫기, 초기화, 적용 버튼에는 명확한 aria label을 둔다.
- SVG 아이콘은 장식용과 의미 전달용을 구분한다.

## 5. 백엔드 개발 작업

백엔드는 화면에서 하드코딩하던 데이터를 API로 제공해야 한다. 특히 필터는 화면별로 직접 박아 넣으면 유지보수가 어렵기 때문에 메타데이터 API를 우선 설계한다.

필요 API:

```text
GET  /api/vehicles
GET  /api/vehicle-filters
GET  /api/commercial-filters
GET  /api/vehicle-options
GET  /api/regions
GET  /api/dealers
POST /api/vehicles/plate-lookup
POST /api/uploads/media
POST /api/vehicle-drafts
POST /api/vehicles
```

필터 메타데이터 예시:

```json
{
  "groups": [
    {
      "id": "manufacturer",
      "label": "제조사",
      "type": "multi-select",
      "searchable": true,
      "options": [
        {
          "id": "hyundai",
          "label": "현대",
          "count": 1284,
          "iconUrl": "/assets/brands/hyundai.svg"
        }
      ]
    },
    {
      "id": "price",
      "label": "가격",
      "type": "range",
      "unit": "만원",
      "min": 0,
      "max": 20000,
      "step": 100
    }
  ]
}
```

화물/특장 트리 예시:

```json
{
  "id": "cargo",
  "label": "화물",
  "children": [
    {
      "id": "cargo-truck",
      "label": "카고(화물)트럭",
      "children": [
        {
          "id": "cargo-truck-1t",
          "label": "1톤",
          "payloadRange": {
            "minTon": 1,
            "maxTon": 1
          }
        }
      ]
    }
  ]
}
```

매물등록 payload 예시:

```json
{
  "category": "used-car",
  "plateNumber": "14러0927",
  "manufacturerId": "hyundai",
  "modelId": "grandeur",
  "year": 2022,
  "mileageKm": 32000,
  "fuelType": "gasoline",
  "color": "black",
  "priceManwon": 3150,
  "options": ["navigation", "rear-camera", "heated-seat"],
  "dealerId": "dealer-001",
  "mediaIds": ["media-001", "media-002"]
}
```

백엔드 체크포인트:

- 필터 count는 선택 조건에 따라 갱신 가능한 구조로 제공한다.
- 트럭/특장 필터는 승용 필터와 별도 스키마를 허용한다.
- 지역, 매매단지, 상사명, 딜러명은 분리된 필드로 내려준다.
- 차량번호 조회 실패, 중복 등록, 임시저장, 이미지 업로드 실패 상태를 API 응답으로 명확히 구분한다.
- 등록폼 드롭다운 값은 백엔드 코드값과 프론트 표시명을 분리한다.

## 6. Codex가 대행하는 방식

Codex가 실제 반영 작업을 대행할 경우, 아래 순서로 진행하는 것이 가장 안전하다.

1. 실제 프론트엔드/백엔드 저장소 접근
   - 프론트엔드 저장소 URL
   - 백엔드 저장소 URL
   - 실행 명령어
   - 환경변수 샘플
   - 테스트 계정
   - 스테이징 API 주소

2. 코드베이스 진단
   - 라우팅 구조 확인
   - 디자인 시스템 확인
   - 기존 필터/검색/등록 컴포넌트 확인
   - API 호출 계층 확인
   - 이미지/아이콘 에셋 관리 방식 확인

3. PR 단위 분리
   - PR 1: 공통 디자인 토큰, 아이콘 에셋, GNB/Header 정리
   - PR 2: 중고차 리스트 필터, 카드, 상단 칩, 바텀시트
   - PR 3: 화물/특장 리스트와 트리 필터
   - PR 4: 승용차 매물등록폼
   - PR 5: 상세 페이지 아이콘, 옵션, 미디어 컨트롤
   - PR 6: 통합 QA 및 회귀 수정

4. 개발 중 검증
   - Playwright로 데스크톱/모바일 스크린샷 비교
   - 필터 클릭, 드롭다운, 슬라이더, 바텀시트 조작 테스트
   - URL query 동기화 테스트
   - API mock과 실제 API 모두 테스트
   - 콘솔 에러, 레이아웃 깨짐, 스크롤 sticky 문제 확인

5. 최종 반영
   - 개발진 리뷰
   - 기획자/디자이너 확인
   - 스테이징 배포
   - 운영 배포 전 QA 체크리스트 완료

## 7. 개발진 역할 분담

프론트엔드:

- 시안의 HTML/CSS를 제품 컴포넌트로 재구성한다.
- GNB, 필터, 바텀시트, 리스트 카드, 등록폼을 공통 컴포넌트화한다.
- SVG/PNG/WebP 에셋의 표시 크기와 정렬 기준을 통일한다.
- 모든 필터 조작을 실제 URL query 및 API 요청과 연결한다.
- 모바일 바텀시트와 데스크톱 좌측 필터가 동일하게 동작하는지 검증한다.

백엔드:

- 승용차 필터와 화물/특장 필터 메타데이터 API를 제공한다.
- 화물/특장/버스 유형 및 적재용량 트리를 마스터 데이터로 관리한다.
- 차량번호 조회, 임시저장, 등록, 이미지 업로드 API를 안정화한다.
- 딜러명, 상사명, 지역, 매매단지를 분리된 필드로 내려준다.
- 필터별 count와 선택 조건 반영 로직을 설계한다.

기획/디자인:

- GNB 최종 메뉴명과 순서를 확정한다.
- 중고차, 수입차, 트럭-특장, 바이크, 캠핑카, 부품·용품의 카테고리 체계를 확정한다.
- 필터 항목의 최종 표시명과 노출 순서를 확정한다.
- 등록폼의 필수/선택 항목을 카테고리별로 확정한다.
- 아이콘 원본 파일과 매핑표를 확정한다.

QA:

- 각 필터 항목을 하나씩 클릭해 열림, 선택, 해제, 초기화, 적용 동작을 확인한다.
- 스크롤 시 좌측 필터, 상단 칩, 바텀시트가 흔들리거나 잘리지 않는지 확인한다.
- 차량번호 조회부터 등록 완료까지 전체 플로우를 확인한다.
- 데스크톱, 태블릿, 모바일 해상도에서 레이아웃을 확인한다.
- 시안과 실제 구현의 타이포그래피, 간격, 색상, 그림자, 버튼 곡률을 비교한다.

## 8. 오픈 이슈

- 실제 운영 프론트엔드의 프레임워크와 디자인 시스템 확인 필요
- 백엔드의 현재 필터 API 구조 확인 필요
- 차량번호 조회 API 또는 외부 연동 제공 범위 확인 필요
- 화물/특장 마스터 데이터의 최종 원본 확정 필요
- 브랜드 아이콘의 라이선스 및 원본 파일 관리 방식 확인 필요
- 매물등록 카테고리에서 승용, 바이크, 트럭/특장, 캠핑카, 건설기계, 중고부품의 최종 범위 확정 필요
- 딜러, 중고차딜러, 딜러검색 등 메뉴명은 최종 GNB 확정 전까지 변경 가능성 있음

## 9. 완료 기준

아래 항목이 충족되면 시안 반영 1차 완료로 본다.

- 메인 GNB와 카테고리 구조가 최종 메뉴 기준으로 반영되어 있다.
- 중고차 리스트의 좌측 필터, 상단 필터칩, 바텀시트가 같은 상태로 동작한다.
- 화물/특장 리스트에서 형식/적재용량 트리가 엔카 기준 구조와 동일한 방식으로 열린다.
- 엔카 진단/보증 등 플랫폼 전용 필터는 제거되어 있다.
- Airbnb 렌터카 스타일의 체크박스, 선택 칩, 버튼, 바텀시트 톤이 반영되어 있다.
- 매물등록 승용차 폼에서 차량번호 `14러0927` 조회 이후 주요 입력 항목이 정상 동작한다.
- 판매가격 `만원` 단위, 드롭다운, 슬라이더, 스크롤 sticky, 텍스트 줄바꿈 문제가 해결되어 있다.
- 모든 주요 화면에서 콘솔 에러가 없다.
- 데스크톱/모바일 스크린샷 QA가 완료되어 있다.

## 10. 실서버 적용 예상 기간

아래 일정은 현재 제작된 시안을 실제 프론트엔드/백엔드 저장소에 반영하고, 스테이징 검수 후 운영 배포 가능한 수준까지 끌어올리는 기준이다. 정확한 기간은 실제 코드베이스 구조, 기존 API 완성도, 디자인 시스템 유무, 배포/리뷰 속도에 따라 달라진다.

### 10.1 Codex 단독 대행 기준

프론트엔드와 백엔드 저장소 접근, 로컬 실행 환경, 스테이징 계정, API 문서가 준비되어 있다는 전제에서는 약 3~5주가 필요하다.

| 단계 | 작업 | 예상 기간 |
| --- | --- | --- |
| 1 | 실제 저장소 구조 파악, 실행 환경 구성, 현재 서비스 코드 진단 | 1~2일 |
| 2 | 공통 디자인 토큰, GNB, 헤더, 아이콘 에셋 정리 | 2~3일 |
| 3 | 중고차 리스트 필터, 상단 필터칩, 좌측 필터, 바텀시트 이식 | 4~6일 |
| 4 | 화물/특장 필터 트리, 형식/적재용량 depth, 상용차 전용 필터 구현 | 4~7일 |
| 5 | 승용차 매물등록폼, 차량번호 조회, 드롭다운, 바텀시트, 가격 입력, 사진 등록 연동 | 5~8일 |
| 6 | 차량 상세 아이콘, 옵션, 미디어 컨트롤, 상세 정보 정리 | 2~4일 |
| 7 | 데스크톱/모바일 QA, 스크롤/sticky/슬라이더/드롭다운 회귀 수정 | 3~5일 |
| 8 | PR 리뷰 반영, 스테이징 배포, 운영 배포 전 안정화 | 2~5일 |

총합:

- 빠른 경우: 약 15영업일
- 보통 예상: 약 20~25영업일
- API/데이터 정리가 부족한 경우: 약 30영업일 이상

### 10.2 개발진 병행 기준

프론트엔드 1~2명, 백엔드 1명, QA/기획 검수자가 병행하고 Codex가 구현 보조, PR 작성, QA 자동화, 회귀 수정까지 맡는 방식이면 약 2~3주로 단축 가능하다.

| 역할 | 병행 작업 |
| --- | --- |
| Codex | 시안 분석, 컴포넌트 이식, PR 생성, 테스트 자동화, 회귀 수정 |
| 프론트엔드 | 제품 코드 리뷰, 디자인 시스템 적용, 상태 관리 구조 확정 |
| 백엔드 | 필터 메타데이터 API, 화물/특장 트리 API, 등록 API 정리 |
| 기획/디자인 | 문구, 메뉴명, 필터명, 카테고리 체계 확정 |
| QA | 브라우저별 기능 점검, 모바일/데스크톱 회귀 테스트 |

총합:

- 빠른 경우: 약 10영업일
- 보통 예상: 약 12~15영업일
- API 변경과 데이터 정제가 큰 경우: 약 20영업일

### 10.3 일정상 가장 큰 변수

가장 시간이 많이 흔들리는 부분은 아래 4개다.

- 화물/특장/버스 유형과 적재용량 트리의 최종 마스터 데이터 확정
- 차량번호 조회 API와 등록폼 API의 실제 응답 구조
- 좌측 필터, 상단 필터칩, 바텀시트를 하나의 상태로 묶는 기존 프론트 구조
- 이미지 업로드, 임시저장, 등록 완료까지의 운영 정책

따라서 실서버 반영을 빠르게 하려면 먼저 `중고차 리스트 + 승용차 매물등록`을 1차 배포 범위로 잡고, `화물/특장 트리 필터`는 별도 PR로 병렬 진행하는 방식이 가장 현실적이다.

## 11. 개발진에게 요청할 자료

Codex가 실제 개발 대행을 바로 시작하려면 아래 자료가 필요하다.

- 프론트엔드 GitHub 저장소
- 백엔드 GitHub 저장소
- 로컬 실행 방법
- `.env.example`
- 스테이징 API 주소
- 테스트 계정
- 차량번호 조회 테스트 데이터
- 현재 운영 필터 API 문서
- 매물등록 API 문서
- 이미지 업로드 정책
- 아이콘/브랜드 에셋 원본 폴더
- 배포 및 PR 리뷰 규칙

이 자료가 준비되면 Codex는 실제 저장소에서 브랜치를 나누고, 위 PR 단위로 구현/검증/수정까지 대행할 수 있다.
