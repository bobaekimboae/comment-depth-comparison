# 게시판 보기 방식 지시문

## 1. 목표

보배드림형 게시판 목록에서 사용자가 보기 방식을 선택할 수 있게 한다. 1차 구현 대상은 `목록형 · 썸네일 없음`이다.

## 2. 상단 보기 방식 컨트롤

- 기존 보기 아이콘 우측에 현재 유형 텍스트와 아래 화살표를 붙인다.
- 형식: `[보기 아이콘] 목록형 ▾`
- 썸네일 제거 모드 선택 시 상단 표기는 `썸네일 없음 ▾`으로 변경한다.
- 버튼을 누르면 레인드롭 모바일 웹처럼 작은 팝오버가 아니라, 현재 구현 기준에서는 하단 바텀시트를 연다.

## 3. 보기 방식 바텀시트

보기 방식 섹션에는 아래 항목을 둔다.

1. `목록형 · 기본`
   - 기존 네이버 카페형 목록 구조 유지
   - 썸네일이 있는 게시물은 우측 썸네일 표시

2. `목록형 · 썸네일 없음`
   - 모든 게시물에서 썸네일 영역을 제거
   - 제목/메타 영역이 썸네일이 있던 자리까지 확장
   - 댓글 배지는 기존 위치와 디자인 유지
   - 썸네일 제거 시에도 신규 글 도트, 계급 배지, 닉네임, 시간, 조회수는 유지

3. `피드형`
   - 추후 피드형 게시판 시안용

4. `앨범형`
   - 추후 이미지 중심 게시판 시안용

5. `동영상만`
   - 추후 영상 게시물 필터형 시안용

## 4. CSS 구현 규칙

기본 목록형:

```css
.item {
  grid-template-columns: minmax(0, 1fr) auto minmax(46px, auto);
}
```

썸네일 없음 목록형:

```css
.item.noThumb {
  grid-template-columns: minmax(0, 1fr) minmax(46px, auto);
}
```

핵심 규칙:

- 썸네일 DOM을 숨기는 수준이 아니라 렌더링하지 않는다.
- 썸네일 칸이 사라진 만큼 제목 컬럼이 확장되어야 한다.
- 댓글 배지는 우측 끝에 유지한다.
- 제목은 기존과 동일하게 최대 2줄 클램프를 유지한다.

## 5. JS 동작 규칙

```js
var textMode = mode === 'text' || !display.thumbnail;
var showThumb = !textMode && post.thumb;
```

- `목록형 · 썸네일 없음` 선택 시 `mode='text'`, `display.thumbnail=false`로 둔다.
- `목록형 · 기본` 선택 시 `mode='list'`, `display.thumbnail=true`로 복구한다.
- 리스트 표시 옵션에서 `썸네일 이미지` 토글을 끄면 자동으로 `목록형 · 썸네일 없음` 상태와 동일하게 동작한다.

## 6. 검수 기준

- 썸네일 없음 모드에서 제목이 썸네일 위치까지 확장되는가
- 댓글 배지의 위치, 높이, 라운드, 숫자 정렬이 기본 목록형과 같은가
- 한 줄 제목과 두 줄 제목 모두 깨지지 않는가
- 썸네일 있는 게시물과 없는 게시물이 같은 폭 규칙으로 정렬되는가
- 보기 방식 선택 후 상단 라벨이 즉시 바뀌는가
- 페이지 새로고침 후 기본 목록형으로 정상 렌더링되는가
